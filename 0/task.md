# fkhost Stabilization Tasks

- [x] **1. App Configuration**
  - [x] Update `productName` to `fkhost`
  - [x] Update `bundle.identifier` to `com.fkhost.app`
- [x] **2. Window Control Fix**
  - [x] Apply `-webkit-app-region: drag` ONLY to non-interactive titlebar areas
  - [x] Ensure buttons have `-webkit-app-region: no-drag`
- [x] **3. Stop Button & Process Safety**
  - [x] Add Frontend Stop Button + disable UI on `connecting`
  - [x] Store process globally `Arc<Mutex<Option<Child>>>`
  - [x] Prevent duplicate starts
  - [x] Stop safely (`kill()`, `wait()`, wipe state)
  - [x] App close handling (auto-kill process)
- [x] **4. Custom Domain Integration (Production Mode)**
  - [x] Add Tunnel Mode selector (Quick / Named)
  - [x] Add domain + tunnel_id inputs for Named mode
  - [x] Handled in `spawn_tunnel` via `TunnelMode::Named`
- [x] **5. Static Folder Serving**
  - [x] Internal HTTP server (axum) targeting selected directory
  - [x] "Static Folder" picker via `openDialog`
  - [x] Server stop logic via `server_shutdown_tx` oneshot
- [x] **6. Port Handling**
  - [x] User-defined port in left panel config
  - [x] Quick presets: React (5173), Next.js (3000)
  - [ ] `TcpListener` pre-bind check before starting (still missing)
- [x] **7. Log Panel Stability**
  - [x] Add ALL / INFO / ERROR filters
  - [x] Add Copy logs
  - [x] Add Auto-scroll toggle
  - [ ] Limit history array bounds (still missing)
- [ ] **8. Dark / Light Mode**
  - [ ] Add UI Toggle button in titlebar
  - [ ] Bind `data-theme` to localStorage via `index.html` pre-render script
- [ ] **9. Session Persistence**
  - [ ] Cache port, domain, and last tunnel URL to localStorage
  - [ ] Display last URL in idle state
- [x] **10. State Sync & Timeout**
  - [x] 15s timeout in watchdog for URL detection
  - [x] Strict IPC event schema (`pipeline_step`)
- [x] **11. Cloudflared pre-check**
  - [x] `ensure_cloudflared` handles missing binary + downloads
  - [ ] `cloudflared --version` explicit version check (not yet wired)
